serenity {
    take.screenshots = FOR_FAILURES
    restart.browser.for.each = scenario
;     Cấu hình wait dùng cho explicit wait (WebElementFacade, WaitUntil…)
    webdriver.wait.for.timeout = 30000
;     Cấu hình implicitly wait dùng để tìm phần tử
    webdriver.timeouts.implicitlywait = 30000


}

headless.mode = true



 environments {

    staging_chrome {
        webdriver.base.url = "https://serenity-bdd.github.io/"
        admin_username = "hang_automationHR"
        admin_passwork = "Hang95@***"

        webdriver {
          driver = chrome

          capabilities {
            browserName = "chrome"
            acceptInsecureCerts = true
            "goog:chromeOptions" {
              args = ["-headless","remote-allow-origins=*","test-type", "no-sandbox", "ignore-certificate-errors", "--window-size=1000,800",
                "incognito", "disable-infobars", "disable-gpu", "disable-default-apps", "disable-popup-blocking",
                "disable-dev-shm-usage", "disable-extensions", "disable-web-security", "disable-translate", "disable-logging"]
            }
          }
        }

    }

     staging_firefox {
;             webdriver.base.url = "http://localhost/orangehrm/web/index.php"
            webdriver.base.url = "https://serenity-bdd.github.io/"
            admin_username = "hang_automationHR"
            admin_passwork = "Hang95@***"

            webdriver {
              driver = firefox
              capabilities {
                              browserName = "firefox"
                              acceptInsecureCerts = true
                              "moz:firefoxOptions" {
                                args = ["-headless"]
                              }
                          }
            }

        }


  }
